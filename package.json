{
  "name": "mockserver",
  "version": "0.2.0",
  "description": "Servidor simulado para prática do protocolo de comunição HTTP (Arquitetura de rede Cliente-Server)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx serve -p 3000 ./dist"
  },
  "author": "Cauã Alves",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "express": "^5.2.1",
    "serve": "^14.2.6",
    "typescript": "^6.0.3"
  }
}
